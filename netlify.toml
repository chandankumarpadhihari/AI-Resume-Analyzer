[build]
  command = "npm install"
  functions = "functions"
  publish = "."

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"